Могут ли переменные в JavaScript хранить больше одного значения? Могут!

Предположим у нас есть список дел:
```js
let todos = ["сварить сына", "вырастить борщ", "посадить дом"];
```
Чтобы JS знал где начитается и заканчивается значение - его нужно обернуть в квадратные скобки

Допустим, что дел у нас добавилось
```js
todos.push("построить дерево");
```

Мы можем получить какое-то дело по его *индексу* (номеру элемента, начиная с 0):
```js
let todos = [  
    "сварить сына", // 0  
    "вырастить борщ", // 1  
    "посадить дом" // 2  
];

console.log(todos[0]); // сварить сына  
console.log(todos[1]); // вырастить борщ  
console.log(todos[2]); // посадить дом
```

Можем получить только часть дел:
```js
// 1 - начало
// 3 - конец (не включительно)
console.log(todos.slice(1, 3)); // [ 'вырастить борщ', 'посадить дом' ]
```

Удалить последний элемент
```js
todos.pop()
console.log(todos); // [ 'сварить сына', 'вырастить борщ', 'посадить дом' ]
```

Получить индекс элемента
```js
console.log(todos.indexOf("сварить сына")); // 0
```

Удалить элементы по индексу
```js
// 1 - индекс с которого начнется удаление
// 2 - количество элементов для удаления
todos.splice(1, 2);
console.log(todos); // [ 'сварить сына', 'построить дерево' ]
```

Сделаем что-нибудь из этого

## 1. Отобразим массив
**index.html**
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Todo</title>
    <script src="script.js" defer></script>
</head>
<body>
    <ul id="todos"></ul>
</body>
</html>
```

**script.js**
```js
let todos = [
    "сварить сына", 
    "вырастить борщ",
    "посадить дом",
    "построить дерево"
];

const list = document.getElementById("todos");

function renderTodos() {
    todos.forEach(function (todo) {
        const li = document.createElement("li");
        li.innerText = todo;
        list.appendChild(li);
    })
}

renderTodos();
```

>`forEach` - применяет функцию ко всем элементам

Мы создаем анонимную функцию (без имени), которая создает элемент li, кладет в него текст элемента массива и добавляет в элемент на странице

## 2. Добавление элементов

**index.html**
```html
<!DOCTYPE html>  
<html lang="en">  
<head>  
    <meta charset="UTF-8">  
    <title>Todo</title>  
    <script src="script.js" defer></script>  
</head>  
<body>  
    <!-- Добавление элементов -->  
    <div>  
        <label for="todo-input">Добавить в список дел:</label>  
        <input type="text" id="todo-input">  
        <button id="add-button" onclick="addTodo()">Добавить</button>  
    </div>  
  
    <ul id="todos"></ul>  
</body>  
</html>
```

**script.js**
```js
let todos = [
    "сварить сына", 
    "вырастить борщ", 
    "посадить дом", 
    "построить дерево"
];

const list = document.getElementById("todos");
const todoInput = document.getElementById("todo-input"); // <--

function renderTodos() {
    todos.forEach(function (todo){
        const li = document.createElement("li");
        li.innerText = todo;
        list.appendChild(li);

    })
}

// Новые функции
function clearTodos() {
    list.innerHTML = "";
}

function addTodo(){
    clearTodos(); // Очищаем список
    todos.push(todoInput.value); // Добавляем элемент в массив
    renderTodos(); // Отображаем массив
}

renderTodos();
```

## 3.  Удаление элементов
```js
let todos = [  
    "сварить сына",  
    "вырастить борщ",  
    "посадить дом",  
    "построить дерево"  
];  
  
const list = document.getElementById("todos");  
const todoInput = document.getElementById("todo-input");   
  
function clearTodos() {  
    list.innerHTML = "";  
}  
  
// на каждом элементе будет кнопка
function renderTodos() {  
    clearTodos()  
    todos.forEach(function (todo, index) {
        const li = document.createElement("li");  
        const deleteButton = document.createElement("button");  
  
        deleteButton.onclick = function () {  
            todos.splice(index, 1);  
            renderTodos();  
        }; 
        deleteButton.innerText = "🗑️";  
        li.innerText = todo;  
        li.appendChild(deleteButton);  
        list.appendChild(li);  
    })}  
  
function addTodo(){  
    clearTodos();  
    todos.push(todoInput.value); 
    renderTodos();
}  
  
renderTodos();
```

# Задания

📖 - задание на самостоятельное изучение
⭐️ -  сложное задание

1. **Доработка примера из конспекта**
	1. Добавьте оформление css
		1. 📖 ⭐️ Добавьте разные темы и смену тем
	2. 📖 Используйте html формы для ввода данных
	3. ⭐️ Через localStorage сделайте сохранение списка дел
	4. Пусть при добавлении элемента на нем записывается когда по времени произошло добавление
2. **Новые примеры**
	1. Ютуб
		1. ⭐️ Создайте массив, в котором каждый элемент - массив, который хранил бы даннные о видео (картинка, название)
		2. Отобразите массив массивов
		3. Добавьте оформление
			1. Сделайте разные темы
			2. ⭐️ 📖 Сделайте, чтобы сайт хорошо смотрелся на разных экранах (изучите css grid minmax)
	2. База данных пользователей
		1. ⭐️ Создайте массив массивов, который бы хранил данные о пользователях (фио, возраст, пол)
		2. Отобразите массив
		3. ⭐️ Реализуйте добавление новых пользователей
			1. Реализуйте сохранение пользователей через localStorage
3. Свободная теме. Сделайте что-угодно с использованием нового материала. Гуглить можно. Нейросети - нельзя
---
# ⭐️ Дополнительная тема
## Массивы и функции

**Решите задачу:**
Пусть у вас есть массив чисел `a`. Создайте массив `b`, в котором все элементы из `a` массива будут возведены во вторую степень

Скорее всего у вас получилось что-то на подобие:
```js
const a = [1,2,3,4,5]
let b = []

for (let i = 0; i < a.length; i++) {
    b.push(a[i] ** 2)
}

console.log(b) // [ 1, 4, 9, 16, 25 ]
```

И этот код работает, но его можно улучшать. Многим программистам не хочется держать в голове то, что a это массив и мы должны обращаться к нему по индексу. Было бы очень удобно, если бы мы могли использовать `a[i]` как переменную. Мы можем!

```js
const a = [1,2,3,4,5]
let b = []
for (el of a) {
    b.push(el ** 2)
}
console.log(b)
```

Теперь мы можем использовать el вместо `a[i]` и забыть о том, что мы имеем дело с массивами

Все замечательно и этот код работает, но программисты так часто перебирали массивы через циклы, что им надоело и они добавили методы для переборов массивов

```js
const a = [1,2,3,4,5]
let b = []

a.forEach((el) => b.push(el ** 2))

console.log(b)
```

foreach принимает в себя функцию, которую будет применять к каждому элементу

Но и тут программисты придумали как облегчить себе жизнь. Если мы хотим получить массив из значений массива, то лучше будет использовать **map**

```js
const a = [1,2,3,4,5]
let b = a.map((el) => el ** 2)

console.log(b)
```

map это то же самое что и foreach, но возвращает массив в качестве результата

**Другое задание**
У вас есть тот же массив a. Составьте массив из четных его элементов

У вас получилось что-то на подобие:
```js
const a = [1,2,3,4,5]  
let b = []  
a.forEach((el) => {if (el % 2 == 0){
		b.push(el)
	}
})  
  
console.log(b)
```

Но и тут программисты выпендрились и придумали filter
```js
const a = [1,2,3,4,5]
let b = a.filter((el) => el % 2 == 0)
console.log(b)
```

Если функция, которую мы напишем в filter для элемента вернет true, то он будет добавлен в массив

**Последнее задание:**
У вас есть тот же список `a`, найдите сумму его элементов

```js
const a = [1,2,3,4,5]
let sum = 0

a.forEach(element => {sum += element})
console.log(sum)
```

Или же так:
```js
const a = [1,2,3,4,5]

const sum = a.reduce((sum, el) => sum + el, 0)
console.log(sum)
```
мы передаем в reduce
- функцию, где первое значение - *аккумулятор* (занимается сбором) и второе значение - сам элемент
- Начальное значение аккумулятора (у нас в примере это 0)

| Метод  | Когда его использовать                        |
| ------ | --------------------------------------------- |
| map    | Получить массив того же размера               |
| filter | Получить массив меньшего размера              |
| reduce | Получить 1 значение из всех элементов массива |
